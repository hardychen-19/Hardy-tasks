# Hardy Tasks project instructions

When a user supplies a course spreadsheet or asks to create Hardy Tasks schedules or tasks, read and follow `.codex/skills/hardy-plan-bridge/SKILL.md`.

- Let the model interpret ambiguous intent and produce the compact plan JSON.
- Use the bridge script for extraction, validation, preview, mutation, backup through the existing CLI, and verification.
- Never hand-edit `/Users/Shared/QuietTasks/*.json`.
- Require confirmation after preflight and before apply.
- Do not add deletion to the bridge.
